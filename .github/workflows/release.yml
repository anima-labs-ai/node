name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      # Trusted Publishing (OIDC) — no NPM_TOKEN. npm >= 11.5.1 exchanges the
      # workflow's id-token for a short-lived credential and attaches provenance
      # automatically (so no --provenance flag / NODE_AUTH_TOKEN). Requires an
      # npm trusted publisher configured for @anima-labs/sdk:
      # GitHub Actions -> anima-labs-ai/node -> release.yml.
      - name: Publish to npm (trusted publishing)
        run: |
          npm install -g npm@latest
          npm publish --access public
